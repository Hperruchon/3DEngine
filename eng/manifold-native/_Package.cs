// Packaging placeholder. This project ships only the native manifoldc payload under
// runtimes/<rid>/native/ (see Engine.Geometry.Manifold.Native.csproj); the compiled assembly
// is excluded from the package via IncludeBuildOutput=false. This file exists solely
// so the SDK has a compile input.
